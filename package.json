{
  "name": "landing-pages",
  "version": "1.0.0",
  "scripts": {
    "start:tailwind": "cross-env NODE_ENV=development tailwindcss --postcss -i src/tailwind.css -o tailwind-runtime.css -w",
    "build:tailwind": "cross-env NODE_ENV=production tailwindcss --postcss -i tailwind.css -o tailwind-runtime.css"
  },
  "devDependencies": {
    "autoprefixer": "^10.0.0",
    "cross-env": "^7.0.0",
    "postcss": "^8.0.0",
    "tailwindcss": "^3.4.15"
  }
}
